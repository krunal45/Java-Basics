/*
Sure, here's a beginner-level coding question based on the object concept in Java:

Imagine you're building a simple banking system. Define a Java class called `Account` with the following attributes:

1. `accountNumber` (a unique identifier for each account)
2. `accountHolderName` (the name of the account holder)
3. `balance` (the current balance in the account)

Include a constructor to initialize these attributes and methods to perform the following actions:

1. `deposit(double amount)`: This method should add the specified amount to the account's balance.
2. `withdraw(double amount)`: This method should subtract the specified amount from the account's balance, if sufficient funds are available.
3. `getBalance()`: This method should return the current balance of the account.

Now, here's the question:

Using the `Account` class you've defined, create two account objects, perform some deposit and withdrawal operations
on each account, and display the final balance for each account.
 */
//package objectBasics;
//
//public class Account {
//    private int accountNumber;
//    private String accountHolderName;
//    private double balance;
//
//    Account(int accountNumber, String accountHolderName, int balance) {
//        this.accountNumber = accountNumber;
//        this.accountHolderName = accountHolderName;
//        this.balance = balance;
//    }
//
//    private void deposit(double amount) {
//        this.balance = this.balance + amount;
//    }
//
//    private void withdraw(double amount){
//        if (amount<)
//        this.balance = this.balance - amount;
//    }
//
//}
